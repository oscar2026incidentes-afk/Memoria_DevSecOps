Tarea 1.
    1. Crea un Dockerfile que partiendo de una imagen PHP genera una imagen que:
        1. Copia una aplicación en PHP a un directorio del contenedor. Esta aplicación se debe copiar directamente desde un directorio del anfitrión (PPSv5). Para facilitar las cosas, debe de ser una aplicación sencilla que no emplee bases de datos (ya que si no también habría que instalar un MySQL).
